# TODO

## Web Client / TUI Parity Gaps

- [ ] **Admin panel** — TUI has full admin panel (`a` key). Web client has the keyboard command registered but no handler or UI yet.
- [ ] **SSH key authentication** — TUI supports SSH key auth with auto-registration. Web client only has password-based auth.
- [ ] **Server discovery / multi-server** — TUI has server browsing. Web client has basic URL input only.
