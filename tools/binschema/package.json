{
  "name": "binschema",
  "version": "0.1.0",
  "description": "Bit-level binary serialization schema and code generator",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/run-tests.js",
    "test:filter": "npm run build && node dist/run-tests.js --filter",
    "watch": "tsc --watch"
  },
  "keywords": [
    "binary",
    "serialization",
    "codegen",
    "protocol"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
